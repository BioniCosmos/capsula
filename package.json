{
  "name": "capsula",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test:bytecode": "cd test && BACKEND=bytecode cargo run",
    "test:qbe": "cd test && BACKEND=qbe cargo run",
    "test": "bun test:bytecode && bun test:qbe"
  },
  "dependencies": {
    "@msgpack/msgpack": "^3.1.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^7"
  }
}
